# Gitleaks configuration
# Conformance tests and documentation - intentionally committed
# Auto-generated by wrknv security.generate
# https://github.com/gitleaks/gitleaks

[extend]
useDefault = true

[[allowlists]]
description = "Conformance tests and documentation - intentionally committed"
paths = [
    '''conformance/.*$''',
    '''docs/.*$''',
    '''test_[^/]*\.py$''',
]
